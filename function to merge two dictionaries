def merge_dicts(dict1, dict2):
    merged = dict1.copy()  
    for key, value in dict2.items():
        merged[key] = value  
    return merged

# Example
dict_a = {"a": 1, "b": 2}
dict_b = {"b": 3, "c": 4}

merged_dict = merge_dicts(dict_a, dict_b)
print(merged_dict)
