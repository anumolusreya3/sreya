def char_frequency(s):
    freq = {}  
    for char in s:
        if char in freq:
            freq[char] += 1
        else:
            freq[char] = 1
    return freq

# Example
string = "hello world"
frequency = char_frequency(string)
print(frequency)
